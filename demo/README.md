# Demo

Showcase assets for the project — the place to drop:

- architecture and wiring diagrams
- Qt HMI screenshots / screen recordings
- hardware photos (Raspberry Pi gateway, ESP32 nodes, CAN wiring)
- ESP32 serial logs and decoded CAN bus logs
- a short end-to-end demo video (sensor → gateway → HMI → cloud)

These are linked from the top-level [README](../README.md) so the repo reads as a
complete product at a glance.

> Status: scaffold — add media as the system comes online.
